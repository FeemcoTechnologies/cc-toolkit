# =============================================================================
# Kali Command Center - Core Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt
# Python 3.10+ recommended
#
# Versions pinned with `==` to what is verified working on the Kali build
# (2026-08). Pinning keeps SBOM/SCA scans (syft/grype) accurate: unpinned
# `>=` ranges let syft catalog packages but prevents grype from pinning exact
# versions, hiding real vulns in the toolkit's own dependency graph.
# =============================================================================

# --- Web Dashboard ---
flask==3.1.3

# --- Config / Playbook parsing ---
PyYAML==6.0.3

# --- Encrypted credential store ---
cryptography==47.0.0

# --- DNS resolution (tool_wrappers: dns_track) ---
dnspython==2.7.0

# --- MCP (Model Context Protocol) server ---
mcp==1.28.1

# --- WebSocket terminal (optional, graceful fallback if missing) ---
websockets==16.0

# --- PDF report generation (optional, graceful fallback if missing) ---
weasyprint==69.0

# --- DOCX report generation (optional, graceful fallback if missing) ---
python-docx==1.2.0

# --- Network scanning / traceroute (optional, graceful fallback if missing) ---
scapy==2.7.0

# --- Network interface utilities (optional, graceful fallback if missing) ---
netifaces==0.11.0

# --- Jupyter notebook execution (optional, used in MCP tools & CLI) ---
papermill==2.7.0

# --- Interactive TUI dashboard (optional but recommended) ---
prompt_toolkit==3.0.52

# --- System resource monitoring (used by TUI monitor screen) ---
psutil==7.1.0

# --- Optional: Arsenal command cheatsheet integration ---
# arsenal  # uncomment if you use Orange-Cyberdefense/arsenal
