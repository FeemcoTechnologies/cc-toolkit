# EDR Server Environment Variables
# Copy this to .env and fill in values before running podman-compose up -d

# Secret key for agent HMAC authentication (generate with: openssl rand -hex 32)
EDR_SECRET_KEY=CHANGE_ME

# API key for admin/dashboard access (generate with: openssl rand -hex 16)
EDR_ADMIN_API_KEY=CHANGE_ME
